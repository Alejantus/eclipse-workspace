package aleatorios;

import java.util.Random;

import lectura.Leer;

public class Aletorios {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		int min=0,max=0;
		
		Random aleatorio = new Random (System.nanoTime());
		
		for (int i = 0; i < dinero.length; i++) {
			if(i==0) {
				menor = dinero[i]
			}
			if(dinero[i]>mayor) {
				mayor = dinero[i]
			}
			if(dinero[i]<menor) {
				menor = dinero[i]
			}
		}
		
		System.out.println("El máximo del array es: "+mayor);
		System.out.println("El mínimo del array es: "menor);

	}

}
